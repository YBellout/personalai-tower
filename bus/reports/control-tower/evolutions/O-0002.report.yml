order: O-0002
outcome: partial
mode: design
stories:
  S-016: { result: design_proposed, commit: d3c69d4, file: "AI Control Tower/docs/stories/S-016-promote-rollback-design.md", note: "Yassir's three picks (see decided[]) are written into the working tree but not yet committed, blocked, see notes[]." }
  S-018: { result: not_started }
  S-019: { result: not_started }
  S-020: { result: not_started }
  S-017: { result: not_started }
  S-021: { result: not_started }
  S-022: { result: not_started }
  S-023: { result: not_started }
  S-013: { result: not_started, note: "M3, last per order_of_work" }
files_touched: ["AI Control Tower/docs/stories/S-016-promote-rollback-design.md"]
cost_usd: 0
decided:
  - "S-016: promote and rollback are Yassir-triggered, not dispatched by run-operations.sh; D-017 scopes the runner to unattended jobs only"
  - "S-016, D1, picked by Yassir 2026-09-21: promote proceeds to the smoke check when health.tests is empty, rather than refusing outright"
  - "S-016, D2, picked by Yassir 2026-09-21: pruning past five archived snapshots is not a self-governance delete, no derogation needed"
  - "S-016, D3, picked by Yassir 2026-09-21: promote refuses with a named reason when health.smoke is empty, rather than skipping verification"
needs:
  - "control room: fold D1 to D3 above into a numbered decision on its own writes (decisions/, registry, self-governance reading are shared files); not done here per D-018"
  - "control room, or a later session with delete working: clear the evolutions clone's stuck .git/HEAD.lock and .git/index.lock so the fold-in-picks commit can land"
next: "Blocked on the two items above before S-016 can close. S-018 (status.py) is next per O-0002's order_of_work and does not depend on either."
notes:
  - "the evolutions clone's .git/HEAD.lock survived a first commit's own cleanup failure (delete not permitted on this connected folder), then blocked the next commit outright. device_request_delete_permission reported success for ~/Documents/PersonalAI, but rm and mv both still failed with Operation not permitted on .git/HEAD.lock and, once git status ran again, .git/index.lock too. Stopped retrying per the git-locks rule rather than risk more stuck locks. Nothing lost: d3c69d4 (the original design) is committed; the fold-in-picks edit is saved in the working tree, uncommitted."
  - "flagged to Yassir directly in this chat; needs either the delete grant actually taking effect, or the control room clearing the stale locks by hand next time it holds the hub."
