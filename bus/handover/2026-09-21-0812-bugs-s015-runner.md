# Handover, 2026-09-21 08:12, Bugs chat

**What.** Built S-015, the runner dispatcher and its installer, order O-0001.
Code ready to merge; four of its five criteria stay open, needing the real Mac.

**What changed.**
- add "AI Control Tower/dev/bin/run-operations.sh", on branch bugs, commit
  bca67d3. The dispatcher launchd will start via PersonalAI Runner.app. Writes
  _tower/bus/heartbeat/alive on every wake (liveness, git-ignored), runs
  heartbeat.sh, and captures any job's stderr, not its exit code, into
  _tower/bus/heartbeat/last-error (heartbeat.sh already reports its own routine
  outcomes elsewhere). Adding a job is one run_job line.
- add "AI Control Tower/launchers/install-runner.command", same commit. Builds
  ~/Applications/PersonalAI Runner.app with osacompile, repoints the existing
  ai.personalai.heartbeat launchd agent at it, runs it once in the foreground so
  macOS asks its two questions.
- edit _tower/claims/control-tower.bugs.yml, added S-015's two paths alongside
  S-014's (both kept, released only on merge).
- edit _tower/bus/reports/control-tower/bugs/O-0001.report.yml, S-015 section:
  C4 closed, C1/C2/C3/C5 open with reasons, and the ordered Mac steps only
  Yassir can do.

**Verified before writing.** Same reads as the S-014 handover, plus: this
session's shell has no osacompile and no launchd (it is a sandbox alongside the
real Mac's files, not the Mac's own shell), so C1, C2, C3 and C5 cannot be
produced here regardless of code correctness. Tested what is code-level: a
scratch harness ran the dispatcher clean, captured a deliberately broken job's
stderr, then took a new no-op job with one added line and no other change,
closing C4.

**What it opens.** Nothing further in this order; both its stories are now
reported. The Mac steps in the report are what opens next, for Yassir.

**What is left.** Yassir: launchers/install-runner.command (steps in the
report), then leave the Mac alone for about an hour. Control room: merge both
commits, then verify C1/C3/C5 against alive and events.log, and C2 by asking
Yassir what he saw in Privacy and Security.

**Read first next time.** this file, then
bus/reports/control-tower/bugs/O-0001.report.yml.
