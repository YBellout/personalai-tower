# Handover, 2026-09-20 18:25

**What.** S-012, the UI server basis. Flagged first that v0.1.0-plan.md says "no UI
is built" in M0; Yassir answered via the control room's interactive intake (D-007):
build now as dev scaffolding, the Control Tower's own status UI, Python/Flask +
static HTML. Not an M0 story, doesn't touch the exit test.

**What changed.**
- new  "AI Control Tower/dev/ui/" (app.py, templates/index.html, static/style.css,
       requirements.txt, README.md)
- new  "AI Control Tower/launchers/start-ui.command"
- new  "AI Control Tower/docs/S-012-ui-server-design.md" (design, accepted same day)
- edit evolution/control-tower.md, "Pulled forward from M1" section
- edit status/status.json (generated_at refreshed; M0 content unchanged, re-verified)

**Verified, not assumed.** Root discovery (reads root: from config.yml, same pattern
as dev/bin/heartbeat.sh) resolved correctly. Flask installed in this session's own
sandbox only, to smoke-test: GET / renders the real status.json under the fixed
layout (headline, actions, stories, milestone all present), GET /api/status returns
it as JSON. Not run on the real Mac yet, that happens when Yassir double-clicks the
launcher.

**Also found while regenerating status (D-015), not part of this story.** git remote
is configured locally (origin -> YBellout/personalai-tower, from the D-002 work
earlier today) but this VM can't push or fetch, credential-osxkeychain is
unreachable from here exactly as v0.1.0-plan.md's ground truth says. No evidence of
a successful push from any environment yet (no origin/main ref, no diagnostic report
in bus/reports/). S-002's three remaining actions (create repo, create token, run
connect-github.command) are unchanged and still open.

**What is left.** Yassir: the same three S-002 actions, unrelated to this story.
Claude: nothing further on S-012 until Yassir has looked at the UI or M1 starts.

**Note.** "AI Control Tower" has no git repo of its own yet (S-002's note: "_tower
and AI Control Tower each get one, M0 does the first"), so dev/ui, the launcher and
the design file are not yet under version control anywhere.

**Read first next time.** knowledge/status.md, then this file.
