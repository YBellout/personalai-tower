# Handover, 2026-09-20

**What.** Wrote docs/OPEN_SESSION_PROMPT.md, the paste block that opens a new AI
Control Tower chat.

**Why.** Yassir asked for a copy and paste prompt to onboard a fresh chat. M0's
scheduled task has to be created from inside the real project chat so its runs land
there, so he needs a clean way to open one.

**What changed.** new docs/OPEN_SESSION_PROMPT.md. Nothing else.

**What it proves.** D-007 Rule 1 exercised for the first time: the block carries the
marker line, so a session receiving it executes it as written instead of running it
through intake.

**What is left.** It is a hand-written stand-in for bin/brief.sh, which M1 generates.
It is almost all pointers, so the state of play stays in the newest handover entry
and the prompt itself does not go stale. Delete it when brief.sh exists.

**Blocked on Yassir.** Unchanged: D-001 options A to E, D-002 GitHub account, D-003
mirror destination, X-001 grant or deny, D-005 iExecAdmin, D-006 stray-scan folder
grants, root path confirmation.

**Read first next time.** docs/LAUNCH_PLAN.md.
