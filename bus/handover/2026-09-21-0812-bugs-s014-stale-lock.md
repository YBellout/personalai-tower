# Handover, 2026-09-21 08:12, Bugs chat

**What.** Built and tested S-014, stale git lock handling in heartbeat.sh, order
O-0001. Ready to merge.

**What changed.**
- edit "AI Control Tower/dev/bin/heartbeat.sh", on branch bugs, workspace
  "AI Control Tower/.chats/bugs", commit 61e888b. Adds check_git_locks(), called
  before each of the two `git add` calls: a live lock with git running is left
  alone (exit 8), a stale lock (no git process, >5 min old) is removed and the run
  continues, a young lock with no git running is left alone. Numbered lock
  artifacts are removed silently, logged only. Ref locks are reported, never
  removed.
- add _tower/claims/control-tower.bugs.yml, claiming S-014's path (kept, D-018
  releases claims only on merge).
- add _tower/bus/reports/control-tower/bugs/O-0001.report.yml, S-014 section:
  C1-C4 all closed, evidence named per criterion.

**Verified before writing.** Read D-018, order O-0001, both design files from
origin/main (State: accepted), derogations.yml (X-001 covers heartbeat.sh), every
claims file (none held this path), self-governance.md, operator.md, the newest
prior handover. git fetch on the bugs clone before the gate check.

**What it opens.** S-015, second in this order's order_of_work, independent of
S-014.

**What is left.** Control room: merge this commit into main. Note found in
passing, not a story of mine: the bugs clone's own .git left stray
index.lock/HEAD.lock/tmp_obj_* files it could not clean up itself after normal
git operations in this session's sandbox (unlink was blocked until I requested
delete permission for PersonalAI); removed them by hand so the clone stays
usable. Not S-014's bug, and not seen on real launchd runs; naming it in case it
recurs.

**Read first next time.** this file, then the S-015 handover from the same
session, then bus/reports/control-tower/bugs/O-0001.report.yml.
