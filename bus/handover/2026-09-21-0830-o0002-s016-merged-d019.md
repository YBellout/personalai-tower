# Handover, 2026-09-21 08:30, control room

**What.** The Evolutions chat reported S-016's design, and it was stuck: two stale git
locks in its clone blocked the commit of Yassir's three picks. Cleared, committed at its
request, merged. The picks are recorded as D-019. The runner is still not installed.

**Found at 12:22Z.** No liveness file, one Mac run ever: install-runner.command has not
been run. Evolutions: report O-0002, one handover, claims released, clone holding the
uncommitted edit, `.git/HEAD.lock` from 12:15 and `.git/index.lock` from 12:19, and a
permission probe file. Its delete permission had reported success and still refused
every rm and mv. It stopped and asked, which was correct.

**What the control room did.**
- Waited until index.lock was past five minutes, checked no git process was running,
  then removed both locks: S-014's own rule applied by hand. Removed the probe file.
- Committed the chat's edit on branch evolutions, c2348f8, with the control room's
  identity and a message saying the content is the chat's and why the control room
  committed it.
- Merged into main, febc99c. The branch changed one file, checked against the
  merge-base. The design stays proposed until Yassir accepts it.
- new decisions/D-019.md, Yassir's three picks: no tests means straight to smoke; its
  own snapshots past five are pruned without a derogation, and nothing else is; no smoke
  check means promote refuses. Consequence noted on S-021: smoke.sh must exist first.
- edit docs/CHAT_PROMPT_BUGS.md and CHAT_PROMPT_EVOLUTIONS.md: before any git, request
  delete permission and prove it with a probe, and run no git if the probe fails.
  Takes effect for chats opened from now on. The two running chats do not reread it.
- edit evolution/control-tower.md, status/status.json.
- committed the chats' files: the O-0002 report and the Evolutions handover.

**Finding worth keeping.** A work chat's git works only if delete permission actually
takes effect in its session. It did for the Bugs chat and did not for the Evolutions
chat, same folder, same morning. Each git commit then leaves locks and the next one
fails. The prompt now makes that visible at the start rather than mid-commit.

**What is left.** Yassir: the runner install and the hour, Privacy and Security, accept
S-016, GitHub. Evolutions: S-018 next per its order.

**Read first next time.** this file.
