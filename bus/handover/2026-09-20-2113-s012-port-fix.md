# Handover, 2026-09-20 21:13

**What.** S-012 bug fix. Yassir ran start-ui.command and landed on the Family School
Dashboard instead: port 8787 was already held by that app, Flask failed to bind
("Address already in use"), but the launcher's shell-side `open` call fired anyway
and pointed the browser at 8787 regardless, where the other app was already
listening. Confirmed from his own terminal output, not guessed.

**What changed.**
- edit "AI Control Tower/dev/ui/app.py": added find_free_port() (probes from 8787
  upward, 30 tries), the app now opens its own browser tab via webbrowser.open()
  only after it has actually bound a port, the shell no longer guesses the URL
- edit "AI Control Tower/launchers/start-ui.command": dropped the blind
  `open http://127.0.0.1:8787`, app.py handles it now
- edit "AI Control Tower/dev/ui/README.md": says to watch the terminal for the
  actual URL instead of assuming 8787

**Verified, not assumed.** Simulated the exact collision in this session's own
sandbox (bound 8787 first, then ran find_free_port()): it correctly stepped to 8788.

**What is left.** Yassir: run start-ui.command again and confirm the page it opens
now says "AI Control Tower", not "Family School Dashboard".

**Read first next time.** knowledge/status.md, then this file.
