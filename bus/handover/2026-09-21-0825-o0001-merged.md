# Handover, 2026-09-21 08:25, control room

**What.** Both work chats are running. The Bugs chat finished order O-0001 and reported.
The control room reviewed its code, reran its key tests independently, and merged it.

**The two chats, as found at 08:15.**
- Bugs, session 014NMbYb1C8Daycez9DrUbwo: claimed S-014 then S-015 before touching
  anything, worked only in its clone, committed 61e888b and bca67d3 on branch bugs,
  wrote one report and two handover files, never touched main or a shared file.
- Evolutions, session 01XGyggeceZjgr8Lrh7Unamr: claimed S-016's design file at 08:10,
  writing it in its clone. No report yet.

**Review before merge.**
- The branch changed exactly heartbeat.sh, run-operations.sh and install-runner.command,
  checked against the merge-base. A plain diff against main showed v0.2.0-plan.md
  deleted and the acceptance lines reverted, only because the branch forked before
  those commits. The merge-base check is what proved a merge would not revert them.
- Rerun by the control room in a scratch copy, not taken from the report: S-014 C4 no
  lock, exit 4, handover unchanged; C3 young lock, exit 8, lock kept; C2 lock 10 min
  old, removed, commit made. Dispatcher: alive written, exit 0, last-error empty.
  bash -n passes on all three files, all three mode 755.
- Merged into main: ebe0c8d. v0.2.0-plan.md and both acceptance lines intact after.

**Four findings from the review. None blocks the merge or S-004.**
- S-024, bug: a lock found by heartbeat.sh's second check, after a successful push,
  overwrites that run's handover with "SKIPPED".
- S-025, bug: run-operations.sh empties last-error every wake, so an error lasts 30
  minutes and can vanish unseen.
- For S-021: the runner app has the dev/ dispatcher path built into it, and a rebuild
  loses the macOS grant. Promotion to prod/ must repoint it without a rebuild. Noted
  on S-021 in the evolution map.
- If a run ever hangs, for example on a Keychain prompt at night, `open -a` will not
  start a second instance, so every later wake does nothing. The dead-man check catches
  it: knowledge/status.md now reads bus/heartbeat/alive's age on every regeneration.

**What changed.**
- "AI Control Tower" main: merge ebe0c8d.
- edit evolution/control-tower.md   S-014 done, S-015 merged and awaiting the install,
                                    S-024 and S-025 unscheduled, S-021 constraint.
- edit knowledge/status.md          step 7: alive's age, 🔴 past 60 minutes; last-error
                                    to Watch. Closes the gap until status.py, S-018.
- edit status/status.json           regenerated.
- committed the work chats' files: both claims files, the O-0001 report, the two Bugs
  handovers.
- Claims left as the chats wrote them: D-018 has each chat release its own after merge.

**What is left.** Yassir: install-runner.command, Allow twice, an hour untouched, then
tell the control room what Privacy and Security shows. Then the GitHub steps. Control
room: verify S-015 C1, C3, C5 against alive and events.log.

**Read first next time.** this file.
